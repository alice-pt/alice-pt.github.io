const images = document.querySelectorAll("#image");
const describes = document.querySelectorAll("#describes");

images.forEach(image => {

    document.addEventListener("mouseover", () => {
       .classList.remove("hidde")
    })
})